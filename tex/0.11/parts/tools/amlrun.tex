%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode

\chapter[The Runtime System -- amlrun]{The Runtime System -- \lstinline!amlrun!}
\label{ch:tools-amlrun}

This chapter will describe the \ToolAmlrun tool, which reads files output by \ToolAmlc (\sref{ch:tools-amlc}) and executes a specified executable\footnote{Due to be described later, but those could be e.g. a specific function, or a script file.}.






